package services

import (
// "fmt"
)

// "reflect"
// "strings"

func FindDeptSr(email string) {
	//23ucs145@kamarajengg.edu.in
	// roll_no := strings.Split(email, "@")[0]
	// dept := roll_no[0]

	// for i := 0; i < len(roll_no); i++ {
	// 	// if(reflect.TypeOf(string(roll_no[i]))!=reflect.TypeOf(145) && i!='u'){
	// 	// 	dept+=i
	// 	// }
	// }
	// cs,ad,bt,ec,it,me,mt

	// dept := fmt.Sprintf("%c"+"%c", email[3], email[4])
	// fmt.Println("dept - ", dept)
	// switch dept {
	// case "cs":
	// 	dept_table=
	// }
}
